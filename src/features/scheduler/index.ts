// Feature: Session Scheduler (schedule study sessions with calendar UI)
//
// Slice structure:
//   scheduler/
//   ├── components/   — Calendar, SessionCard, CreateSessionModal
//   ├── hooks/        — useScheduler.ts
//   ├── api/          — scheduler.api.ts
//   └── types/        — scheduler.types.ts (Session, Recurrence)
